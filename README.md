# Set-up Script for Aws/Gcp
A Simple Script to setup build environment and install dependencies Files Written in Bash.

## Usage:
```bash
curl -s https://raw.githubusercontent.com/Adarsh0127-Elite/Setup_script/main/bash.sh | sudo bash
```

## Requirements:
- ```curl``` and ```jq``` must be installed.
